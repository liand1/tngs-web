
// 文件类型枚举
export enum FileType {
  Directory = 'directory',
  File = 'file',
  Image = 'image',
  Video = 'video',
  Audio = 'audio',
  Document = 'document',
  Archive = 'archive',
  Other = 'other',
}

// export interface FilesModel extends Array<FileModel> { }
